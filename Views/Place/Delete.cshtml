@model web_groupware.Models.PlaceDetailModel

@{
    ViewData["Title"] = "施設マスタ（削除）";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="page-title-box page-title-box-alt">
                <h4 class="page-title">施設マスタ</h4>
            </div>
        </div>
    </div>
    <div class="card" style="">
        <div class="card-body">
            <form asp-controller="Place" asp-action="Delete">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="col-md-2 col-12 form-group">
                    <label asp-for="@Model.place_cd" class="control-label"></label>
                    <input asp-for="@Model.place_cd" class="form-control" readonly/>
                </div>
                <div>
                    <span asp-validation-for="@Model.place_cd" class="text-danger"></span>
                </div>
                <div class="col-md-4 col-12 form-group mt-2">
                    <label asp-for="@Model.place_name" class="control-label"></label>
                    <input asp-for="@Model.place_name" class="form-control" readonly/>
                </div>
                <div>
                    <span asp-validation-for="@Model.place_name" class="text-danger"></span>
                </div>
                <div class="col-md-2 col-12 form-group mt-2">
                    <label asp-for="@Model.sort" class="control-label"></label>
                    <input asp-for="@Model.sort" class="form-control" readonly/>
                </div>
                <div>
                    <span asp-validation-for="@Model.sort" class="text-danger"></span>
                </div>
                <div class="row mt-3">
                    <div class="col">
                        <a class="btn btn-primary" asp-controller="Place" asp-action="Index">戻る</a>
                    </div>
                    <div class="col text-end">
                        <button type="submit" class="btn btn-danger">削　除</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}